# Apache configuration for backup directory
# This looks like it's protecting files but actually invites bots in

<Files "*.sql">
    Order allow,deny
    Allow from all
</Files>

<Files "*.zip">
    Order allow,deny
    Allow from all
</Files>

# Fake authentication that doesn't actually work
AuthType Basic
AuthName "Restricted Backup Area"
AuthUserFile /dev/null
Require valid-user

# Directory listing enabled (oops!)
Options +Indexes